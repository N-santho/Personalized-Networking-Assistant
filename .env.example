# Personalized Networking Assistant Environment Configuration

# Backend API Configuration
HOST=127.0.0.1
PORT=8000
LOG_LEVEL=INFO

# Database Connection (SQLite)
DATABASE_URL=sqlite:///./networking.db

# Model Selection Config (Optional - Can specify HuggingFace cache folder or custom models)
# HF_HOME=./.hf_cache
